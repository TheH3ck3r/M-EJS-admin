import React from "react";
export type PortalProps = {
    /**
     * Контент
     */
    children?: React.ReactNode;
    /**
     * Функция, возвращающая контейнер, в который будут рендериться дочерние элементы
     */
    takePortalContainer?: () => Element;
    /**
     * Немедленно отрендерить дочерние элементы (false -- контент будет отрендерен на следующий ререндер).
     */
    immediateMount?: boolean;
};
export declare const Portal: React.ForwardRefExoticComponent<PortalProps & React.RefAttributes<Element>>;
