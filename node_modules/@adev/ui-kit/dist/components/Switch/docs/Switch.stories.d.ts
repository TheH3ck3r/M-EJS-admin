import React from "react";
import { StoryObj } from "@storybook/react";
import Switch from "../Switch";
declare const _default: import("@storybook/types").ComponentAnnotations<import("@storybook/react/dist/types-0a347bb9").R, import("../../../utils/defaultProps").defaultProps & {
    view?: string | undefined;
    name?: string | undefined;
    onChange?: ((event?: React.ChangeEvent<Element> | undefined) => void) | undefined;
    textOn?: React.ReactNode;
    textOff?: React.ReactNode;
    checked: boolean;
    disabled?: boolean | undefined;
    inputHtmlId?: string | undefined;
} & React.RefAttributes<HTMLInputElement>>;
export default _default;
/**
 * Свитч это аналог Checkbox. Для применения выбранного состояния не нужно совершать дополнительных действий, таких как отправка формы или нажатия кнопки «применить». Свитч может располагаться слева или справа от контента.
 *
 * Если свитч нельзя использовать, его следует заблокировать. Валидация свитчей не требуется.
 */
export declare const HowToUse: StoryObj<typeof Switch>;
/**
 * Свитч может находиться в выбранном и невыбранном состоянии. В редких кейсах его значение может быть недоступным для редактирования.
 */
export declare const State: StoryObj<typeof Switch>;
/**
 * Свитч может быть отображен в разных цветах. Есть заранее определенные: `primary`, `secondary` и `accent`. В приложениях рекомендуется использовать только эти предопределенные значения, но если вам требуется кастомизировать свитч, то вы можете указать нужный вам цвет.
 */
export declare const ColorTemplate: StoryObj<typeof Switch>;
