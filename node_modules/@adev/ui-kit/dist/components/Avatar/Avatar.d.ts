import { FC, ReactNode } from "react";
import { defaultProps } from "../../utils/defaultProps";
export type AvatarPropsType = defaultProps & {
    /**
     * Скруглен ли аватар
     */
    rounded?: boolean;
    /**
     * Ссылка на изображение аватара
     */
    avatarUrl?: string;
    /**
     * Имя (Аватар будет сформирован из первых букв, если нет изображения)
     */
    text: string;
    /**
     * Размер аватара
     */
    size?: "xs" | "sm" | "md" | "lg" | "xl" | "xxl";
    /**
     * Автоматический выбор цвета, если нет изображения
     */
    autoColor?: boolean;
    topAddons?: ReactNode;
    bottomAddons?: ReactNode;
};
/**
 * Компонент для создания пользовательских аватаров. Это может быть фотография, значок или первые буквы имени.
 */
declare const Avatar: FC<AvatarPropsType>;
export default Avatar;
