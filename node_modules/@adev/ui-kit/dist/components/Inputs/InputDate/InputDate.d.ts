import { FC } from "react";
import { CalendarProps } from "../../Calendar/Calendar";
export interface InputDateProps extends CalendarProps {
    /**
     * Заголовок инпута
     */
    label: string;
    /**
     * Формат даты (По умолчанию D.MM.YYYY)
     */
    dateFormat?: string;
}
/**
 * Компонент для выбора даты с календарем
 */
declare const InputDate: FC<InputDateProps>;
export default InputDate;
