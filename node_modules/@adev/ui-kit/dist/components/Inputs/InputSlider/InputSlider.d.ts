import React, { ChangeEvent } from "react";
import { SliderProps } from "../../Slider";
import { TextInputProps } from "../BaseInput/BaseInput";
export type InputSliderProps = Omit<TextInputProps, "onChange" | "defaultValue"> & Omit<SliderProps, "onChange" | "defaultValue" | "defaultValueTo"> & {
    defaultValue: number | string;
    onChange: (value: number | "", event?: ChangeEvent<HTMLInputElement>) => void;
};
export declare const InputSlider: React.ForwardRefExoticComponent<Omit<TextInputProps, "onChange" | "defaultValue"> & Omit<SliderProps, "onChange" | "defaultValue" | "defaultValueTo"> & {
    defaultValue: number | string;
    onChange: (value: number | "", event?: ChangeEvent<HTMLInputElement>) => void;
} & React.RefAttributes<HTMLInputElement>>;
