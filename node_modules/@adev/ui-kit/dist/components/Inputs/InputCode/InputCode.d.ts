import { FC } from "react";
export interface IInputCodeProps {
    fields?: number;
    defaultValue?: string;
    onChange?: (value: string) => void;
}
declare const InputCode: FC<IInputCodeProps>;
export default InputCode;
