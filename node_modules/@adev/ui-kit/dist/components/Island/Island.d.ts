import React, { FC } from "react";
import { defaultProps } from "../../utils/defaultProps";
export type IslandPropsType = defaultProps & {
    /**
     * Выравнивание содержимого
     */
    align?: "left" | "center" | "right";
    /**
     * Тема компонента
     *
     * `contrast` - затенение границ
     *
     * `border` - светло-серый контур
     */
    view?: "contrast" | "border";
    /**
     * Переход, который будет выполнен при наведении курсора на блок.
     *
     * `scale` — блок будет плавно увеличен
     * `translateY` — блок будет приподнят вверх
     */
    hoverTransition?: null | "scale" | "translateY";
    /**
     * Обработчик клика по блоку
     */
    onClick?: (event?: React.MouseEvent) => void;
    /**
     * Дочерние элементы компонента
     */
    children?: React.ReactNode;
};
/**
 * Island отображает информацию в виде закругленного блока с рамкой
 */
declare const Island: FC<IslandPropsType>;
export default Island;
