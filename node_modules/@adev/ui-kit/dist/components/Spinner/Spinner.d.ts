import { FC } from "react";
import { defaultProps } from "../../utils/defaultProps";
export type SpinnerPropsType = defaultProps & {
    /**
     * Цвет спиннера
     */
    color?: "primary" | "secondary" | "accent" | string;
    /**
     * Режим spinner
     *
     * - `border` — используйте пограничные блесны для легкого индикатора загрузки.
     * - `growing` — если вам не нравится border spinner, переключитесь на grow spinner. Хотя технически он не вращается, он постоянно растет!
     */
    mode?: "border" | "growing";
    /**
     * Размер спиннера
     */
    size?: "xs" | "sm" | "md" | "lg" | "xl";
};
/**
 * Используется для индикации выполнения команды пользователя.
 */
declare const Spinner: FC<SpinnerPropsType>;
export default Spinner;
