import { ReactElement } from "react";
export type CommonTabsProps = {
    children: Array<ReactElement<CommonTabProps>>;
    defaultValue?: string;
    onChange?: (key: string) => void;
};
export type CommonTabProps = {
    id: string;
    title: string;
    style?: React.CSSProperties;
    className?: string;
    onClick?: (event: React.MouseEvent<HTMLButtonElement>, key?: string) => void;
};
