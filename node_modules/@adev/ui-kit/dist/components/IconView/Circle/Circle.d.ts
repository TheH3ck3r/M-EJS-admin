import React, { ReactNode } from "react";
import { BaseShapeProps } from "../BaseShape";
export type CircleProps = Omit<BaseShapeProps, "pathsMap" | "size"> & {
    /**
     * Размер компонента
     */
    size?: 20 | 24 | 32 | 40 | 48 | 64 | 80 | 128;
    /**
     * Дочерний компонент
     */
    children?: ReactNode;
};
export declare const Circle: React.ForwardRefExoticComponent<Omit<BaseShapeProps, "size" | "pathsMap"> & {
    /**
     * Размер компонента
     */
    size?: 20 | 24 | 32 | 40 | 48 | 64 | 80 | 128 | undefined;
    /**
     * Дочерний компонент
     */
    children?: ReactNode;
} & React.RefAttributes<HTMLDivElement>>;
