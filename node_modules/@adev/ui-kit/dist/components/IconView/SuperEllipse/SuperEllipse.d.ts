import React, { ReactNode } from "react";
import { BaseShapeProps } from "../BaseShape";
export type SuperEllipseProps = Omit<BaseShapeProps, "pathsMap" | "size"> & {
    /**
     * Размер компонента
     * @default 64
     */
    size?: 20 | 24 | 32 | 40 | 48 | 64 | 80 | 128;
    /**
     * Дочерний компонент
     */
    children?: ReactNode;
};
export declare const SuperEllipse: React.ForwardRefExoticComponent<Omit<BaseShapeProps, "size" | "pathsMap"> & {
    /**
     * Размер компонента
     * @default 64
     */
    size?: 20 | 24 | 32 | 40 | 48 | 64 | 80 | 128 | undefined;
    /**
     * Дочерний компонент
     */
    children?: ReactNode;
} & React.RefAttributes<HTMLDivElement>>;
