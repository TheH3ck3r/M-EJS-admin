import { FC, ReactElement } from "react";
import { SegmentProps } from "./Segment/Segment";
export interface SegmentedControlProps {
    /**
     * Дополнительный className
     */
    className?: string;
    /**
     * Обработчик смены выбранного сегмента
     */
    onChange: (id: string | number) => void;
    /**
     * ID выбранного сегмента по умолчанию
     */
    defaultValue: string | number;
    /**
     * Дочерние элементы
     */
    children: Array<ReactElement<SegmentProps>>;
}
/**
 * Используется для переключения или фильтрации контента.
 */
export declare const SegmentedControl: FC<SegmentedControlProps>;
export default SegmentedControl;
